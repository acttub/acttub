# excer

배우를 위한 연습실 지도. acttub 생태계의 일부.

```bash
pnpm install
cp .env.local.example .env.local   # 카카오 / Neon / Clerk 키 채우기
pnpm dev
# http://localhost:3000/excer/
```

## 문서
| 단계 | 위치 |
| --- | --- |
| 아이디어 → 검증 | `outputs/stage-0/` |
| 기획 (PRD 포함) | `outputs/stage-1/` |
| 디자인 (핸드오프 포함) | `outputs/stage-2/` |

자세한 빌드 순서는 `outputs/stage-2/design-handoff.md` 참조.
